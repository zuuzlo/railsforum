<ul class="nav">
  <li>
    <%= link_to "Postit!", root_path, :class => 'brand' %>
  </li>
  <li>
    <%= link_to "All post", posts_path %>
  </li>
</ul>

<% if logged_in? %>
<div class="btn-group nav_item">
      <a href="#" class="btn btn-small dropdown-toggle" data-toggle="dropdown">Categories  
         <span class="caret"></span>
      </a>
      <ul class="dropdown-menu">
        <% @categories.each do |category| %>
          <li><%= link_to category.name, category %><li>
        <% end %>
        <li class="divider"></li>
        <li>          
          <%= link_to '<i class="icon-plus"></i> New Category'.html_safe, new_category_path %>
        </li>
      </ul>
</div>
<div class="nav_item">
  <%= link_to 'New Post', new_post_path, class: "btn btn-success btn-small " %>
</div>
<div class="nav_item">
  <%= link_to(logout_path, class: "btn btn-success btn-small") do %>
  <i class="icon-circle-arrow-left"></i>
  Logout
  <% end %>
</div>
<% else #logged out %>
  <div class="nav_item">
    <%= link_to "Register", register_path, class: "btn btn-success btn-small" %>
  </div>
  <div class="nav_item">
    <%= link_to "Login", login_path, class: "btn  btn-success btn-small" %>
  </div>
<% end %>

